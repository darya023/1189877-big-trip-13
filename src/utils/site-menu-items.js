import {MenuItem} from "../const";

export const generateSiteMenuItems = () => {

  return [
    {
      name: MenuItem.TABLE,
      href: `#`
    },
    {
      name: MenuItem.STATS,
      href: `#`
    }
  ];
};
